<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<schemaDescription xmlns="http://www.quartetfs.com"
                   xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.quartetfs.com ../activepivot-types.xsd">

    <!-- Selection of fields from the datastore -->
    <datastoreSelection baseStore="PortfoliosStore">
        <fields>

            <!-- History Store -->
            <field name="Open"			expression="BaseToHistory/Open" />
            <field name="High"          expression="BaseToHistory/High" />
            <field name="Low"           expression="BaseToHistory/Low" />
            <field name="Close"         expression="BaseToHistory/Close" />
            <field name="Volume"        expression="BaseToHistory/Volume" />
            <field name="AdjustedClose" expression="BaseToHistory/AdjustedClose" />

            <!-- Portfolios Store  -->
            <field name="PortfoliosStockSymbol"/>
            <field name="PortfoliosType"/>
            <field name="StockNumber"   />
            <field name="PositionType"  />
            <field name="Date"/>

            <!-- Sector Store -->
            <field name="CompanyName"	expression="BaseToSector/CompanyName" />
            <field name="Sector"        expression="BaseToSector/Sector" />
            <field name="Industry"      expression="BaseToSector/Industry" />


            <!-- Index Store -->
            <field name="IndexCompanyName"	expression="BaseToIndex/IndexCompanyName"/>
            <field name="IndexStockValue"	expression="BaseToIndex/IndexStockValue" />
            <field name="IndexName"	        expression="BaseToIndex/IndexName" />
            <field name="IndexPositionType" expression="BaseToIndex/PositionType"/>

        </fields>


    </datastoreSelection>

    <!-- List of the ActivePivot instances within that ActivePivot Schema -->
    <activePivots>

        <activePivot id="PerformanceAttributionCube">
            <pivotDescription resource="DESC-INF/PerformanceAttributionCube.xml"/>
        </activePivot>

    </activePivots>

</schemaDescription>